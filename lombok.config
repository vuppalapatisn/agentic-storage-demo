# Stop Lombok from searching parent directories for more config.
config.stopBubbling = true

# Copy @Qualifier onto the constructor parameters that @RequiredArgsConstructor
# generates. Without this, @Qualifier stays on the field only and Spring falls
# back to collection-injection for raw Map/Set beans, injecting the wrong beans.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
